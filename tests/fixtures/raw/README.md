# RAW Fixture Placeholder

`smoke.raw` is used by CI smoke tests to validate the published binary on:

- Linux x64
- Windows x64
- macOS Intel (x64)
- macOS Apple Silicon (arm64)

The committed file is currently an empty placeholder. Replace it with a small
real Thermo `.raw` file to enable end-to-end conversion assertions in CI.
