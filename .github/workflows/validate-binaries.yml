name: Validate Binaries

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
      - master

permissions:
  contents: read

jobs:
  validate:
    uses: ./.github/workflows/reusable-validate-binaries.yml
